<%= form_with(model: habit) do |form| %>
  <% if habit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(habit.errors.count, "error") %> prohibited this habit from being saved:</h2>

      <ul>
        <% habit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :goal %>
    <%= form.number_field :goal %>
  </div>

  <div class="field">
    <%= form.label :time_period %>
    <%= form.number_field :time_period %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :reminders %>
    <%= form.text_field :reminders %>
  </div>

  <div class="field">
    <%= form.label :is_bad %>
    <%= form.check_box :is_bad %>
  </div>

  <div class="field">
    <%= form.label :repeat %>
    <%= form.text_field :repeat %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
